{
  "name": "roaster-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 execute roaster_app --file migrations/001_create_tables.sql && wrangler d1 execute roaster_app --file migrations/002_seed_shoots.sql && wrangler d1 execute roaster_app --file migrations/003_add_indexes.sql && wrangler d1 execute roaster_app --file migrations/004_add_expense_remark.sql",
    "test": "tsx --test tests/**/*.test.ts"
  },
  "dependencies": {
    "itty-router": "^4.0.24"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "@types/node": "^24.10.1",
    "tsx": "^4.11.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.51.0"
  }
}
